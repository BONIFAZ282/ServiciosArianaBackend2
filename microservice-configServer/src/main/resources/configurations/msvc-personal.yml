server:
  port: 9002

spring:
  application:
    name: msvc-personal
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=DBARIANASERVICES;trustServerCertificate=true
    username: codel
    password: "123"

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka